# -----------------------------------------------------------------
# GENERATED by scripts/generate_monitoring_use_cases_app.py — DO NOT EDIT.
# Source of truth: scripts/generate_monitoring_use_cases_app.py.
# Re-run `python3 scripts/generate_monitoring_use_cases_app.py` after edits.
# -----------------------------------------------------------------

[Monitoring Use Cases - Discovery Index Inventory]
description = Captures a bounded inventory of active indexes and rewrites only the metadata_index slice of muc_discovered_datasets.
search = | inputlookup muc_discovered_datasets | search NOT (scan_run_id="inventory_snapshot_latest" AND origin="metadata_index") | append [ | eventcount summarize=false index=* | sort 0 - count | head 512 | eval dataset_key="index::" . index, _key=dataset_key, dataset_id=md5(dataset_key), scan_run_id="inventory_snapshot_latest", display_name=index, origin="metadata_index", indexes=mvappend(index), confidence=0.95, last_seen=strftime(now(),"%Y-%m-%dT%H:%M:%SZ"), _slice_source="current" | fields _key dataset_id scan_run_id dataset_key display_name origin indexes confidence last_seen _slice_source ] | append [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" origin="metadata_index" | eval _slice_source="prior" ] | eval _slice_priority=case(_slice_source="current", 2, _slice_source="prior", 1, true(), 0), _preserve_scope=if(_slice_priority>0, coalesce(dataset_key, _key), _key) | sort 0 _preserve_scope - _slice_priority | dedup _preserve_scope | eval snapshot_source=_slice_source | fields - _slice_priority _preserve_scope _slice_source | outputlookup muc_discovered_datasets append=false
cron_schedule = */30 * * * *
dispatch.earliest_time = -1d@d
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Sourcetype Inventory]
description = Captures a bounded metadata inventory of sourcetypes and rewrites only the metadata_sourcetype slice of muc_discovered_datasets.
search = | inputlookup muc_discovered_datasets | search NOT (scan_run_id="inventory_snapshot_latest" AND origin="metadata_sourcetype") | append [ | metadata type=sourcetypes index=* | sort 0 - totalCount | head 4000 | eval dataset_key="sourcetype::" . sourcetype, _key=dataset_key, dataset_id=md5(dataset_key), scan_run_id="inventory_snapshot_latest", display_name=sourcetype, origin="metadata_sourcetype", sourcetypes=mvappend(sourcetype), confidence=0.85, last_seen=strftime(lastTime,"%Y-%m-%dT%H:%M:%SZ"), _slice_source="current" | fields _key dataset_id scan_run_id dataset_key display_name origin sourcetypes confidence last_seen _slice_source ] | append [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" origin="metadata_sourcetype" | eval _slice_source="prior" ] | eval _slice_priority=case(_slice_source="current", 2, _slice_source="prior", 1, true(), 0), _preserve_scope=if(_slice_priority>0, coalesce(dataset_key, _key), _key) | sort 0 _preserve_scope - _slice_priority | dedup _preserve_scope | eval snapshot_source=_slice_source | fields - _slice_priority _preserve_scope _slice_source | outputlookup muc_discovered_datasets append=false
cron_schedule = 1-59/30 * * * *
dispatch.earliest_time = -1d@d
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Source Inventory]
description = Captures a bounded metadata inventory of sources and rewrites only the metadata_source slice of muc_discovered_datasets.
search = | inputlookup muc_discovered_datasets | search NOT (scan_run_id="inventory_snapshot_latest" AND origin="metadata_source") | append [ | metadata type=sources index=* | sort 0 - totalCount | head 5000 | eval dataset_key="source::" . source, _key=dataset_key, dataset_id=md5(dataset_key), scan_run_id="inventory_snapshot_latest", display_name=source, origin="metadata_source", sources=mvappend(source), confidence=0.55, last_seen=strftime(lastTime,"%Y-%m-%dT%H:%M:%SZ"), _slice_source="current" | fields _key dataset_id scan_run_id dataset_key display_name origin sources confidence last_seen _slice_source ] | append [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" origin="metadata_source" | eval _slice_source="prior" ] | eval _slice_priority=case(_slice_source="current", 2, _slice_source="prior", 1, true(), 0), _preserve_scope=if(_slice_priority>0, coalesce(dataset_key, _key), _key) | sort 0 _preserve_scope - _slice_priority | dedup _preserve_scope | eval snapshot_source=_slice_source | fields - _slice_priority _preserve_scope _slice_source | outputlookup muc_discovered_datasets append=false
cron_schedule = 2-59/30 * * * *
dispatch.earliest_time = -1d@d
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Host Inventory]
description = Captures a bounded metadata inventory of hosts and rewrites only the metadata_host slice of muc_discovered_datasets.
search = | inputlookup muc_discovered_datasets | search NOT (scan_run_id="inventory_snapshot_latest" AND origin="metadata_host") | append [ | metadata type=hosts index=* | sort 0 - totalCount | head 5000 | eval dataset_key="host::" . host, _key=dataset_key, dataset_id=md5(dataset_key), scan_run_id="inventory_snapshot_latest", display_name=host, origin="metadata_host", hosts=mvappend(host), confidence=0.60, last_seen=strftime(lastTime,"%Y-%m-%dT%H:%M:%SZ"), _slice_source="current" | fields _key dataset_id scan_run_id dataset_key display_name origin hosts confidence last_seen _slice_source ] | append [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" origin="metadata_host" | eval _slice_source="prior" ] | eval _slice_priority=case(_slice_source="current", 2, _slice_source="prior", 1, true(), 0), _preserve_scope=if(_slice_priority>0, coalesce(dataset_key, _key), _key) | sort 0 _preserve_scope - _slice_priority | dedup _preserve_scope | eval snapshot_source=_slice_source | fields - _slice_priority _preserve_scope _slice_source | outputlookup muc_discovered_datasets append=false
cron_schedule = 3-59/30 * * * *
dispatch.earliest_time = -1d@d
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Data Model Inventory]
description = Captures a bounded inventory of Splunk data models via REST and annotates CIM-owned models plus acceleration state.
search = | inputlookup muc_discovered_datasets | search NOT (scan_run_id="inventory_snapshot_latest" AND origin="rest_data_model") | append [ | rest splunk_server=local /services/data/models count=0 | eval app_name=coalesce('eai:acl.app', ""), model_name=coalesce(title, modelName, objectName, name) | where isnotnull(model_name) AND model_name!="" | sort 0 model_name | head 256 | eval dataset_key="datamodel::" . model_name, _key=dataset_key, dataset_id=md5(dataset_key), scan_run_id="inventory_snapshot_latest", display_name=model_name, origin="rest_data_model", datamodels=mvappend(model_name), supporting_apps=mvappend(app_name), cim_models=if(match(app_name, "^(Splunk_SA_CIM|SA-CIM)$"), mvappend(model_name), null()), acceleration_state=if(tostring('acceleration.enabled')="1", "accelerated", "not_accelerated"), confidence=if(match(app_name, "^(Splunk_SA_CIM|SA-CIM)$"), 0.80, 0.65), last_seen=strftime(now(),"%Y-%m-%dT%H:%M:%SZ"), _slice_source="current" | fields _key dataset_id scan_run_id dataset_key display_name origin datamodels supporting_apps cim_models acceleration_state confidence last_seen _slice_source ] | append [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" origin="rest_data_model" | eval _slice_source="prior" ] | eval _slice_priority=case(_slice_source="current", 2, _slice_source="prior", 1, true(), 0), _preserve_scope=if(_slice_priority>0, coalesce(dataset_key, _key), _key) | sort 0 _preserve_scope - _slice_priority | dedup _preserve_scope | eval snapshot_source=_slice_source | fields - _slice_priority _preserve_scope _slice_source | outputlookup muc_discovered_datasets append=false
cron_schedule = 4 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Field Sample]
description = Samples bounded events for the current sourcetype inventory and annotates those dataset records with observed field names.
search = | inputlookup muc_discovered_datasets | eval dataset_key=coalesce(dataset_key, _key) | join type=left dataset_key [ | inputlookup muc_discovered_datasets | search origin=metadata_sourcetype scan_run_id="inventory_snapshot_latest" snapshot_source="current" | eval dataset_key=coalesce(dataset_key, _key), sourcetype=mvindex(sourcetypes, 0) | where isnotnull(sourcetype) AND sourcetype!="" | sort 0 sourcetype | head 24 | fields dataset_key sourcetype | map maxsearches=24 search="search earliest=-24h@h latest=now sourcetype=\"$sourcetype$\" | head 200 | fieldsummary | sort 0 - count | head 20 | stats values(field) as fields by sourcetype | eval dataset_key=\"$dataset_key$\", field_sample_source=\"fieldsummary\", field_sample_size=200, field_sample_limit=20 | fields dataset_key fields field_sample_source field_sample_size field_sample_limit" ] | outputlookup muc_discovered_datasets append=false
cron_schedule = 5 * * * *
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Telemetry Snapshot]
description = Writes bounded stage-level telemetry into muc_scan_logs for the latest discovery snapshot.
search = | makeresults | eval stage_rows="inventory_indexes,Index inventory;inventory_sourcetypes,Sourcetype inventory;inventory_sources,Source inventory;inventory_hosts,Host inventory;inventory_cim,Data model inventory;inventory_fields,Field sampling" | appendcols [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" snapshot_source="current" | stats count(eval(origin="metadata_index")) as metadata_index_count count(eval(origin="metadata_sourcetype")) as metadata_sourcetype_count count(eval(origin="metadata_source")) as metadata_source_count count(eval(origin="metadata_host")) as metadata_host_count count(eval(origin="rest_data_model")) as rest_data_model_count count(eval(isnotnull(fields) AND mvcount(fields)>0)) as field_sampled_count ] | makemv delim=";" stage_rows | mvexpand stage_rows | rex field=stage_rows "(?<stage>[^,]+),(?<stage_label>.+)" | eval record_count=case(stage="inventory_indexes", metadata_index_count, stage="inventory_sourcetypes", metadata_sourcetype_count, stage="inventory_sources", metadata_source_count, stage="inventory_hosts", metadata_host_count, stage="inventory_cim", rest_data_model_count, stage="inventory_fields", field_sampled_count, true(), 0) | eval scan_run_id="inventory_snapshot_latest", log_id=scan_run_id . "::" . stage, logged_at=strftime(now(),"%Y-%m-%dT%H:%M:%SZ"), level=if(coalesce(record_count, 0)>0, "info", "warning"), message=stage_label . " produced " . tostring(coalesce(record_count, 0)) . " records in the latest snapshot", details_json="{\"record_count\":" . tostring(coalesce(record_count, 0)) . "}" | fields _key log_id scan_run_id stage level logged_at message record_count details_json | append [ | inputlookup muc_scan_logs | search scan_run_id="inventory_snapshot_latest" level="error" | fields _key log_id scan_run_id stage level logged_at message record_count details_json ] | outputlookup muc_scan_logs append=false
cron_schedule = 6 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0

[Monitoring Use Cases - Discovery Inventory Summary]
description = Writes a latest-snapshot scan_run summary from the current metadata inventory, field sampling, and data-model inspection without deleting prior non-snapshot run records.
search = | inputlookup muc_scan_runs | search NOT scan_run_id=inventory_snapshot_latest | append [ | makeresults | eval scan_run_id="inventory_snapshot_latest", _key=scan_run_id, status="complete", mode="standard", started_at=strftime(now(),"%Y-%m-%dT%H:%M:%SZ"), completed_at=strftime(now(),"%Y-%m-%dT%H:%M:%SZ"), use_cases_direct=0, use_cases_adjacent=0, use_cases_partial=0, use_cases_none=0 | appendcols [ | inputlookup muc_discovered_datasets | search scan_run_id="inventory_snapshot_latest" snapshot_source="current" | stats count as datasets_discovered ] | appendcols [ | inputlookup muc_scan_runs | search scan_run_id="inventory_snapshot_latest" status="complete" | sort 0 - completed_at | head 1 | stats max(datasets_discovered) as prior_datasets_discovered max(use_cases_direct) as prior_use_cases_direct max(use_cases_adjacent) as prior_use_cases_adjacent max(use_cases_partial) as prior_use_cases_partial max(use_cases_none) as prior_use_cases_none ] | appendcols [ | inputlookup muc_scan_logs | search scan_run_id="inventory_snapshot_latest" | stats count(eval(level!="error")) as telemetry_stage_count max(eval(case(level!="error", logged_at, true(), null()))) as latest_telemetry_logged_at max(eval(case(stage="inventory_indexes" AND level!="error", record_count, true(), null()))) as metadata_index_count max(eval(case(stage="inventory_sourcetypes" AND level!="error", record_count, true(), null()))) as metadata_sourcetype_count max(eval(case(stage="inventory_sources" AND level!="error", record_count, true(), null()))) as metadata_source_count max(eval(case(stage="inventory_hosts" AND level!="error", record_count, true(), null()))) as metadata_host_count max(eval(case(stage="inventory_cim" AND level!="error", record_count, true(), null()))) as rest_data_model_count max(eval(case(stage="inventory_fields" AND level!="error", record_count, true(), null()))) as field_sampled_count ] | appendcols [ | inputlookup muc_scan_logs | search scan_run_id="inventory_snapshot_latest" level="error" | sort 0 - logged_at | head 1 | eval logged_error_at=logged_at, logged_error_details=coalesce(details_json, "null") | fields stage message logged_error_at logged_error_details | rename stage as logged_failed_stage message as logged_error_message ] | eval active_logged_failed_stage=if(isnotnull(logged_failed_stage) AND (isnull(latest_telemetry_logged_at) OR latest_telemetry_logged_at="" OR logged_error_at>=latest_telemetry_logged_at), logged_failed_stage, null()), active_logged_error_message=if(isnotnull(active_logged_failed_stage), logged_error_message, null()), active_logged_error_details=if(isnotnull(active_logged_failed_stage), coalesce(logged_error_details, "null"), "null") | eval datasets_discovered_current=coalesce(datasets_discovered, 0), telemetry_stage_count=coalesce(telemetry_stage_count, 0), metadata_index_count=coalesce(metadata_index_count, 0), metadata_sourcetype_count=coalesce(metadata_sourcetype_count, 0), metadata_source_count=coalesce(metadata_source_count, 0), metadata_host_count=coalesce(metadata_host_count, 0), rest_data_model_count=coalesce(rest_data_model_count, 0), field_sampled_count=coalesce(field_sampled_count, 0), logged_error_details=coalesce(logged_error_details, "null"), gap_failed_stage=case(telemetry_stage_count=0, "inventory_telemetry", metadata_index_count=0, "inventory_indexes", metadata_index_count>0 AND metadata_sourcetype_count=0, "inventory_sourcetypes", metadata_sourcetype_count>0 AND metadata_source_count=0, "inventory_sources", metadata_sourcetype_count>0 AND metadata_host_count=0, "inventory_hosts", metadata_sourcetype_count>0 AND rest_data_model_count=0, "inventory_cim", metadata_sourcetype_count>0 AND field_sampled_count=0, "inventory_fields", true(), null()), failed_stage=coalesce(active_logged_failed_stage, gap_failed_stage), status=case(isnotnull(active_logged_failed_stage), "failed", isnotnull(gap_failed_stage), "degraded", true(), status), datasets_discovered=if(isnotnull(failed_stage), coalesce(prior_datasets_discovered, datasets_discovered_current), datasets_discovered_current), use_cases_direct=if(isnotnull(failed_stage), coalesce(prior_use_cases_direct, use_cases_direct), use_cases_direct), use_cases_adjacent=if(isnotnull(failed_stage), coalesce(prior_use_cases_adjacent, use_cases_adjacent), use_cases_adjacent), use_cases_partial=if(isnotnull(failed_stage), coalesce(prior_use_cases_partial, use_cases_partial), use_cases_partial), use_cases_none=if(isnotnull(failed_stage), coalesce(prior_use_cases_none, use_cases_none), use_cases_none), gap_error_message=case(failed_stage="inventory_telemetry", "Stage telemetry produced no records; preserving prior successful snapshot summary where available.", failed_stage="inventory_indexes", "Index inventory produced no records; preserving prior successful snapshot summary where available.", failed_stage="inventory_sourcetypes", "Sourcetype inventory produced no records after index inventory; preserving prior successful snapshot summary where available.", failed_stage="inventory_sources", "Source inventory produced no records after sourcetype inventory; preserving prior successful snapshot summary where available.", failed_stage="inventory_hosts", "Host inventory produced no records after sourcetype inventory; preserving prior successful snapshot summary where available.", failed_stage="inventory_cim", "Data model inventory produced no records after sourcetype inventory; preserving prior successful snapshot summary where available.", failed_stage="inventory_fields", "Field sampling produced no records; preserving prior successful snapshot summary where available.", true(), null()), error_summary=coalesce(active_logged_error_message, gap_error_message) | eval gap_error_details=if(isnotnull(gap_failed_stage), printf("{\"kind\":\"gap\",\"stage\":\"%s\",\"message\":\"%s\",\"telemetry_stage_count\":%s,\"metadata_index_count\":%s,\"metadata_sourcetype_count\":%s,\"metadata_source_count\":%s,\"metadata_host_count\":%s,\"rest_data_model_count\":%s,\"field_sampled_count\":%s}", coalesce(failed_stage, ""), gap_error_message, tostring(telemetry_stage_count), tostring(metadata_index_count), tostring(metadata_sourcetype_count), tostring(metadata_source_count), tostring(metadata_host_count), tostring(rest_data_model_count), tostring(field_sampled_count)), "null"), failure_record_json=case(isnotnull(active_logged_failed_stage), active_logged_error_details, isnotnull(gap_failed_stage), gap_error_details, true(), "null"), summary_json=printf("{\"datasets_discovered\":%s,\"observed_datasets_discovered\":%s,\"telemetry_stage_count\":%s,\"metadata_index_count\":%s,\"metadata_sourcetype_count\":%s,\"metadata_source_count\":%s,\"metadata_host_count\":%s,\"rest_data_model_count\":%s,\"field_sampled_count\":%s,\"failed_stage\":\"%s\",\"error_record\":%s,\"preserved_snapshot\":%s}", tostring(datasets_discovered), tostring(datasets_discovered_current), tostring(telemetry_stage_count), tostring(metadata_index_count), tostring(metadata_sourcetype_count), tostring(metadata_source_count), tostring(metadata_host_count), tostring(rest_data_model_count), tostring(field_sampled_count), coalesce(failed_stage, ""), failure_record_json, if(isnotnull(failed_stage), "true", "false")) | fields _key scan_run_id status mode started_at completed_at failed_stage summary_json datasets_discovered use_cases_direct use_cases_adjacent use_cases_partial use_cases_none error_summary ] | outputlookup muc_scan_runs append=false
cron_schedule = 8-59/30 * * * *
dispatch.earliest_time = -15m@m
dispatch.latest_time = now
enableSched = 1
is_scheduled = 1
disabled = 0
alert.track = 0
action.email = 0
action.logevent = 0
